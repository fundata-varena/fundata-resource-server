package business

import "github.com/fundata-varena/fundata-resource-server/model"

func GetResource(resourceType, id string) *model.ResourceLocal {
	//ops := new(model.ResourceOps)
	//ops.GetResource()
	return &model.ResourceLocal{}
}